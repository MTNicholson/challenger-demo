import { Badge } from "@/components/ui/badge";
import { Card } from "@/components/ui/card";
export function LocationLaunchedChallenges({ challenges, locations }: { challenges:Array<{id:string;title:string;status:string;locationIds:string[]}>; locations:Record<string,string> }){return <Card className="p-6"><div className="flex items-center gap-2"><h2 className="text-xl font-black">Запущенные КТ</h2><Badge variant="warning">{challenges.length}</Badge></div><p className="mt-1 text-sm text-slate-500">Автономные челленджи, опубликованные Flagship-точками.</p><div className="mt-4 space-y-2">{challenges.length?challenges.map(item=><div key={item.id} className="flex items-center justify-between rounded-xl border border-slate-200 p-3"><div><p className="font-black">{item.title}</p><p className="text-sm text-slate-500">Точка: {locations[item.locationIds[0]]??"—"}</p></div><Badge variant={item.status==="active"?"success":"neutral"}>{item.status==="active"?"Активен":item.status}</Badge></div>):<p className="py-5 text-sm font-bold text-slate-400">Челленджей, запущенных точками, пока нет.</p>}</div></Card>}
