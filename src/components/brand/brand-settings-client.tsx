"use client";

import { useMemo, useState, type ChangeEvent, type FormEvent } from "react";
import { useRouter } from "next/navigation";
import { Camera, Save } from "lucide-react";
import { BRAND_CATEGORIES, getBrandCategoryFallback } from "@/lib/brand-visuals";
import { RUSSIAN_CITIES } from "@/lib/russian-cities";
import { BrandAccountSettings } from "@/components/brand/brand-account-settings";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PhoneFrame } from "@/components/user/challenge-phone-preview";

type Brand = { id: string; name: string; category: string | null; city: string | null; address: string | null; description: string | null; website: string | null; logoUrl: string | null; coverImageUrl: string | null; };
type Props = { brand: Brand; memberEmail: string };
type Notice = { ok: boolean; text: string } | null;

function readPreview(file: File, set: (value: string | null) => void) { const reader = new FileReader(); reader.addEventListener("load", () => set(typeof reader.result === "string" ? reader.result : null)); reader.readAsDataURL(file); }
function Notice({ value }: { value: Notice }) { return value ? <p className={value.ok ? "text-sm font-bold text-emerald-700" : "text-sm font-bold text-rose-600"}>{value.text}</p> : null; }

function Preview({ name, category, description, logo, cover, fallback }: { name: string; category: string; description: string; logo: string | null; cover: string | null; fallback: ReturnType<typeof getBrandCategoryFallback> }) {
  return <PhoneFrame className="scale-[0.82] origin-top md:scale-90" resetKey={`${name}-${logo}-${cover}`}><main className="min-h-full bg-[#f5f7fb] p-3 pt-5 text-[#202945]"><div className="overflow-hidden rounded-[20px] border border-[#dbe6f3] bg-white shadow-[0_16px_34px_rgba(48,59,92,.1)]"><div className="h-28 bg-cover bg-center" style={cover ? { backgroundImage: `url(${cover})` } : { background: fallback.coverGradient }} /><div className="p-3"><div className="flex items-start gap-2"><div className="grid h-12 w-12 shrink-0 place-items-center overflow-hidden rounded-xl border border-white bg-cover bg-center text-xl font-black text-white shadow-md" style={logo ? { backgroundImage: `url(${logo})` } : { background: fallback.logoGradient }}>{logo ? null : name.trim()[0]?.toLocaleUpperCase("ru-RU") ?? "Б"}</div><div className="min-w-0 flex-1"><span className="inline-flex rounded-full bg-[#f2f4ff] px-2 py-1 text-[7px] font-black text-[#5965bd]">{category || "Категория"}</span><h2 className="mt-1 truncate text-lg font-black tracking-tight">{name || "Бренд"}</h2></div><span className="text-lg text-[#e2647b]">♡</span></div><p className="mt-3 line-clamp-3 text-[9px] font-semibold leading-4 text-[#68738f]">{description || "Здесь будет описание бренда, его челленджи и награды для гостей."}</p><div className="mt-4 border-t border-slate-100 pt-3"><h3 className="text-sm font-black">Челленджи бренда</h3><div className="mt-2 rounded-xl bg-[#f8f9ff] p-2 text-[9px] font-bold text-[#5965bd]">Задания и награды для гостей</div></div><div className="mt-4 border-t border-slate-100 pt-3"><h3 className="text-sm font-black">Точки бренда</h3><div className="mt-2 rounded-xl bg-[#f8f9ff] p-2 text-[9px] font-bold text-slate-500">Адреса и филиалы</div></div></div></div></main></PhoneFrame>;
}

export function BrandSettingsClient({ brand, memberEmail }: Props) {
  const router = useRouter();
  const [profile, setProfile] = useState({ name: brand.name, category: brand.category ?? "", city: brand.city ?? "", address: brand.address ?? "", description: brand.description ?? "", website: brand.website ?? "" });
  const [logoFile, setLogoFile] = useState<File | null>(null); const [coverFile, setCoverFile] = useState<File | null>(null); const [logoPreview, setLogoPreview] = useState<string | null>(null); const [coverPreview, setCoverPreview] = useState<string | null>(null);
  const [savingProfile, setSavingProfile] = useState(false); const [savingVisuals, setSavingVisuals] = useState(false); const [profileNotice, setProfileNotice] = useState<Notice>(null); const [visualNotice, setVisualNotice] = useState<Notice>(null);
  const fallback = useMemo(() => getBrandCategoryFallback(profile.category), [profile.category]);
  const update = (key: keyof typeof profile, value: string) => { setProfile((state) => ({ ...state, [key]: value })); setProfileNotice(null); };
  const chooseImage = (kind: "logo" | "cover") => (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0] ?? null; if (kind === "logo") { setLogoFile(file); if (file) readPreview(file, setLogoPreview); else setLogoPreview(null); } else { setCoverFile(file); if (file) readPreview(file, setCoverPreview); else setCoverPreview(null); } setVisualNotice(null); };
  async function saveProfile(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSavingProfile(true); const response = await fetch("/api/brand/settings/profile", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(profile) }); const data = await response.json().catch(() => null) as { error?: string } | null; setSavingProfile(false); if (!response.ok) { setProfileNotice({ ok: false, text: data?.error ?? "Не удалось сохранить профиль." }); return; } setProfileNotice({ ok: true, text: "Профиль бренда обновлён." }); router.refresh(); }
  async function saveVisuals(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!logoFile && !coverFile) { setVisualNotice({ ok: false, text: "Выберите новый логотип или промо-картинку." }); return; } const data = new FormData(); if (logoFile) data.set("logo", logoFile); if (coverFile) data.set("coverImage", coverFile); setSavingVisuals(true); const response = await fetch("/api/brand/settings/visuals", { method: "PATCH", body: data }); const result = await response.json().catch(() => null) as { error?: string } | null; setSavingVisuals(false); if (!response.ok) { setVisualNotice({ ok: false, text: result?.error ?? "Не удалось обновить визуалы." }); return; } setLogoFile(null); setCoverFile(null); setVisualNotice({ ok: true, text: "Визуалы бренда обновлены." }); router.refresh(); }
  return <main className="space-y-6"><header><div className="text-sm font-bold text-blue-700">Профиль компании</div><h1 className="mt-1 text-3xl font-black tracking-tight">Настройки бренда</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Обновляйте профиль, визуалы и данные для входа. Точки находятся в отдельной вкладке.</p></header><section className="grid gap-6 xl:grid-cols-[minmax(0,1.1fr)_minmax(360px,.9fr)]"><Card className="p-6"><h2 className="text-xl font-black">Профиль бренда</h2><form className="mt-5 grid gap-4 md:grid-cols-2" onSubmit={saveProfile}><label className="grid gap-2 text-sm font-bold text-slate-600">Название бренда<input className="rounded-xl border border-slate-200 px-4 py-3 font-extrabold text-slate-900 outline-none focus:border-blue-500" value={profile.name} onChange={(event) => update("name", event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-600">Категория<select className="rounded-xl border border-slate-200 px-4 py-3 font-extrabold text-slate-900 outline-none focus:border-blue-500" value={profile.category} onChange={(event) => update("category", event.target.value)}><option value="">Выберите категорию</option>{BRAND_CATEGORIES.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><label className="grid gap-2 text-sm font-bold text-slate-600">Город основного присутствия<select className="rounded-xl border border-slate-200 px-4 py-3 font-extrabold text-slate-900 outline-none focus:border-blue-500" value={profile.city} onChange={(event) => update("city", event.target.value)}><option value="">Выберите город</option>{RUSSIAN_CITIES.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><label className="grid gap-2 text-sm font-bold text-slate-600">Основной адрес<input className="rounded-xl border border-slate-200 px-4 py-3 font-extrabold text-slate-900 outline-none focus:border-blue-500" value={profile.address} onChange={(event) => update("address", event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-600 md:col-span-2">Сайт<input className="rounded-xl border border-slate-200 px-4 py-3 font-extrabold text-slate-900 outline-none focus:border-blue-500" value={profile.website} onChange={(event) => update("website", event.target.value)} placeholder="https://brand.ru" /></label><label className="grid gap-2 text-sm font-bold text-slate-600 md:col-span-2">Описание<textarea className="min-h-28 rounded-xl border border-slate-200 px-4 py-3 font-bold text-slate-900 outline-none focus:border-blue-500" value={profile.description} onChange={(event) => update("description", event.target.value)} /></label><div className="flex flex-wrap items-center gap-3 md:col-span-2"><Button type="submit" disabled={savingProfile}><Save className="h-4 w-4" />{savingProfile ? "Сохраняем..." : "Сохранить профиль"}</Button><Notice value={profileNotice} /></div></form></Card><Card className="overflow-hidden p-0"><div className="grid gap-4 p-6"><div><h2 className="text-xl font-black">Визуалы бренда</h2><p className="mt-1 text-sm leading-6 text-slate-500">Проверьте изменения на карточке бренда до сохранения.</p></div><form className="space-y-4" onSubmit={saveVisuals}><label className="grid gap-2 text-sm font-bold text-slate-600">Новый логотип<input type="file" accept="image/jpeg,image/png,image/webp" onChange={chooseImage("logo")} /></label><label className="grid gap-2 text-sm font-bold text-slate-600">Новая промо-картинка<input type="file" accept="image/jpeg,image/png,image/webp" onChange={chooseImage("cover")} /></label><div className="flex flex-wrap items-center gap-3"><Button type="submit" disabled={savingVisuals}><Camera className="h-4 w-4" />{savingVisuals ? "Загружаем..." : "Сохранить визуалы"}</Button><Notice value={visualNotice} /></div></form></div><div className="h-[470px] overflow-hidden border-t border-slate-100 bg-slate-50 pt-5"><Preview name={profile.name} category={profile.category} description={profile.description} logo={logoPreview ?? brand.logoUrl} cover={coverPreview ?? brand.coverImageUrl} fallback={fallback} /></div></Card></section><BrandAccountSettings initialEmail={memberEmail} /></main>;
}
