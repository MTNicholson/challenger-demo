"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function AdminLoginPage(){const router=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await response.json().catch(()=>null);setLoading(false);if(!response.ok){setError(data?.error??"Не удалось войти");return}router.replace("/adminconsole");}return <main className="adminLogin"><section className="adminLoginCard"><p className="adminMuted">Внутренний кабинет</p><h1 className="adminTitle">Вход администратора</h1><form className="adminForm" onSubmit={submit}><label>Email<input className="adminInput" type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required/></label><label>Пароль<input className="adminInput" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></label>{error&&<div className="adminError">{error}</div>}<button className="adminButton" disabled={loading}>{loading?"Проверяем…":"Войти"}</button></form></section></main>}
