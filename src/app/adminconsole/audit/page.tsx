import { redirect } from "next/navigation";import { getCurrentAdmin } from "@/lib/admin-auth";import { AdminShell } from "../layout";import { AdminList } from "../admin-client";export default async function Audit(){if(!(await getCurrentAdmin()))redirect("/adminconsole/login");return <AdminShell title="Журнал действий"><h1 className="adminTitle">Журнал действий</h1><AdminList kind="audit"/></AdminShell>}
