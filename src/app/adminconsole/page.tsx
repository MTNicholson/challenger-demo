/* eslint-disable @next/next/no-html-link-for-pages */
import { redirect } from "next/navigation";import { AdminShell } from "./layout";import { getCurrentAdmin } from "@/lib/admin-auth";import { prisma } from "@/lib/prisma";
export default async function AdminDashboard(){if(!(await getCurrentAdmin()))redirect("/adminconsole/login");const [brands,pending,challenges,users,locations,recent]=await Promise.all([prisma.brand.count({where:{archivedAt:null}}),prisma.brand.count({where:{status:"pending",archivedAt:null}}),prisma.brandChallenge.count({where:{status:"active"}}),prisma.user.count(),prisma.brandLocation.count(),prisma.adminAction.findMany({take:6,orderBy:{createdAt:"desc"}})]);return <AdminShell title="Dashboard"><h1 className="adminTitle">Обзор платформы</h1><p className="adminMuted">Операционный контроль брендов, челленджей и пользователей.</p><div className="adminGrid">{[["Бренды",brands],["Ожидают подтверждения",pending],["Активные челленджи",challenges],["Пользователи",users],["Точки",locations]].map(([l,v])=><div className="adminCard" key={l as string}><div className="adminMuted">{l}</div><div className="adminMetric">{v}</div></div>)}</div><div className="adminLinks"><a className="adminButton" href="/adminconsole/brands?status=pending">Бренды на проверку</a><a className="adminButton secondary" href="/adminconsole/users">Пользователи</a><a className="adminButton secondary" href="/adminconsole/challenges">Челленджи</a></div><section className="adminCard" style={{marginTop:24}}><h2>Последние действия</h2>{recent.length?<table className="adminTable"><tbody>{recent.map(a=><tr key={a.id}><td>{a.actionType}</td><td>{a.entityType}</td><td>{a.adminEmail}</td><td>{a.createdAt.toLocaleString("ru-RU")}</td></tr>)}</tbody></table>:<div className="adminEmpty">Действий пока нет</div>}</section></AdminShell>}
