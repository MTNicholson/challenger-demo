import { redirect } from "next/navigation";import { getCurrentAdmin } from "@/lib/admin-auth";import { AdminShell } from "../layout";import { AdminList } from "../admin-client";export default async function Users(){if(!(await getCurrentAdmin()))redirect("/adminconsole/login");return <AdminShell title="Пользователи"><h1 className="adminTitle">Пользователи</h1><AdminList kind="users"/></AdminShell>}
