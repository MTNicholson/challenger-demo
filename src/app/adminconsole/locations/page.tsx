import { redirect } from "next/navigation";import { getCurrentAdmin } from "@/lib/admin-auth";import { AdminShell } from "../layout";import { AdminList } from "../admin-client";export default async function Locations(){if(!(await getCurrentAdmin()))redirect("/adminconsole/login");return <AdminShell title="Точки"><h1 className="adminTitle">Точки брендов</h1><AdminList kind="locations"/></AdminShell>}
